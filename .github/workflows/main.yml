name: Cypress Tests

on:
  push:
    branches:
      - main

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    services:
      database:
        image: postgres:latest
        container_name: databaseBook
        env:
          POSTGRES_PASSWORD: ${{ secrets.PASSWORD }}
          POSTGRES_DB: ${{ secrets.DB_NAME }}
          POSTGRES_USER: ${{ secrets.DB_USER }}
        healthcheck:
          test: "pg_isready -U ${{ secrets.DB_USER }}"
          interval: 10s
          retries: 5
        volumes:
          - ./data:/var/lib/postgresql/data
          - ./init.sql:/docker-entrypoint-initdb.d/init.sql
        ports:
          - 5432:5432
        networks:
          - book-net

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker
        uses: docker/setup-buildx-action@v2

      - name: Install Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose

      - name: Build and start Docker Compose services
        run: |
          docker compose up --build -d

      - name: Wait for services to become healthy
        run: |
          docker compose ps &
          sleep 15  # Ensure enough time for the DB to initialize

      - name: Run Cypress Component Tests
        run: |
          echo "Running Cypress Component Tests"
          docker compose run cypress npx cypress run --component

      - name: Run Cypress E2E Tests
        run: |
          echo "Running Cypress E2E Tests"
          docker compose run cypress npx cypress run --e2e

      - name: Tear down Docker Compose
        if: always()
        run: |
          docker compose down --volumes

networks:
  book-net:
    driver: bridge
